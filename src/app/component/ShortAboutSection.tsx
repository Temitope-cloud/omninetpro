import React from "react";
import MarqueeDefault from "./Marquee";
import ScrollReveal from "@/components/ScrollReveal";

const ShortAboutSection = () => {
  return (
    <>
      <MarqueeDefault />
      {/* <ScrollReveal
        baseOpacity={0}
        enableBlur={true}
        baseRotation={5}
        blurStrength={10}
      >
        In a world where every click matters and every second counts, we believe
        that your e-commerce success isn't just about selling products—it's
        about creating experiences that resonate, building connections that
        last, and transforming visitors into loyal customers who become the
        heartbeat of your brand.
      </ScrollReveal> */}
    </>
  );
};

export default ShortAboutSection;
